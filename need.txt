# Download Google Chrome use terminal
wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb





# Settings beautiful terminal
open ~/.bashrc
# .bash_profile 
if [ -n "$BASH" ] && [ -r ~/.bash_profile ]; then 
    . ~/.bash_profile
fi

create ~/.bash_profile
# my setting
PS1='${debian_chroot:+($debian_chroot)}\[\033[01;34m\]🐧 ~> \[\033[00m\]'        

# my aliases
alias lsp='ls -lh --color=always | sed 1d | awk "{printf \"%s %s %s %5s %-30s\n", \$1, \$3, \$4, 
\$5, \$9}"'
alias lsa='ls - lah --color=always | grep -v "^total"'
alias lsl='ls - lh --color=always | grep -v "^total"'

# color fix for other terminals
# enable color support of ls also add handy aliases
if [ -x /usr/bin/dircolors ]; then
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)" 
    alias ls='ls --color=auto'                 
    alias dir='dir --color=auto'                 
    alias grep='grep --color=auto'                 
    alias frep='frep --color=auto'                 
    alias egrep='egrep --color=auto'
fi 

# yeloow color for directories 
# 30 - 37 can change color a folder
LS_DIR_COLOR='di=1;32:'
LS_COLORS=$LS_COLORS$LS_DIR_COLOR

# set terminal title
PROMPT_COMMAND='echo -ne "\033]0;$(pwd)\007"'








# Settings Vim
git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim
curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim

create ~/.vimrc

set nocompatible
filetype off
set rtp+=~/.vim/bundle/Vundle.vim
call vundle#begin()
Plugin 'VundleVim/Vundle.vim'
Plugin 'tpope/vim-fugitive'
Plugin 'git://git.wincent.com/command-t.git'
Plugin 'rstacruz/sparkup', {'rtp': 'vim/'}
Plugin 'flazz/vim-colorschemes'
Plugin 'tpope/vim-surround'
Plugin 'xolox/vim-misc'
Plugin 'xolox/vim-colorscheme-switcher'
Plugin 'tpope/vim-commentary'
call vundle#end()
filetype plugin indent on
call plug#begin('~/.vim/plugged')
Plug 'scrooloose/nerdtree', { 'on': 'NERDTreeToggle' }
Plug 'morhetz/gruvbox'
Plug 'fratajczak/one-monokai-vim'
call plug#end()
syntax on
colorscheme one-monokai " py-darcula, rdark-terminal, one-monokai, OceanicNext, asmdev, afterglow, 
nnoremap <C-_> :Commentary<cr>
set tabstop=4
set shiftwidth=4
set smarttab
set expandtab
set softtabstop=4
set autoindent
set hlsearch     " find world
set incsearch    "пошук по слові та зразу знаходить перше слово
let python_highlight_all = 1
set t_Co=256
set termencoding=utf-8
set termguicolors
set wrap
set linebreak
set encoding=utf-8
set nobackup
set noswapfile
set fileencodings=utf8,cp1251
map <C-n> :NERDTreeToggle<CR>


If Vundle:
PluginInstall
5154624715:AAHUJBTFAvnCYxjDb9Y0sfxh8rlkly_e9vE
If vim-plug:
PlugInstall










# for correct work wifi driver, if don't have drive. Linux Mint + Laptop
lspci -k | grep -iA2 net
sudo apt-get update
sudo apt-get install firmware-b43-installer
sudo apt-get remove bcmwl-kernel-source
sudo reboot




 # Themes pranstarent panel on Linux Mint
 Panel - Numix-Cinnamon-Transparent
 
 
 
 
 
 
 
# Change command CAT on BAT
bat --list-themes # list alowed themes
bat -theme _name_theme_ name-file-you-gonna-open 





# useful command on linux
* some_command --help
* vim 
* ls
* cd
* less
* cat
* head
* tail
* htop
* grep 
* > and >> 
* touch
* mkdir
* rm
* mv
* copy what-copy where-copy
* *.py and ???.py
* chmod
* chown
* ifconfig
* lscpu
'[0-9]' # list number on 0 to 9
